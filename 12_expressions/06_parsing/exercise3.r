### Compare and contrast source() and sys.source().

# `source` is a standardGeneric created from the base package, whereas `sys.source` is a function
# exported from base.

# `source` has many more options than `sys.source`.

# `source` can accept data from connections other than files, whereas `sys.source` cannot.
