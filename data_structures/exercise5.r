# Why is the default (and shortest) NA a logical vector? 
# What's special about logical vectors?

# As in the solution to the previous exercise, logical is the lowest
# on the linear order on types, meaning that it can only get cast
# *up*, and hence cast to any other type.
